---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.23.2-bullseye

inputs:
  - name: dp-permissions-api

caches:
  - path: go/

run:
  path: dp-permissions-api/ci/scripts/lint.sh
