---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.25.1-bookworm

inputs:
  - name: dp-permissions-api

caches:
  - path: go/

run:
  path: dp-permissions-api/ci/scripts/unit.sh
